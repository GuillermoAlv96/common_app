import 'package:flutter/material.dart';
import 'package:login/src/screens/wrapper/view/wrapper_page.dart';

class WrapperRouting extends StatelessWidget {
  final String route = 'wrapper';

  const WrapperRouting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Wrapper();
  }
}
